/**
 * @interface StartTransactionType
 * @param {string} transactionName
 * @param {boolean} bTransactionGroup (optional)
 * @param {boolean} bCanUndo (optional)
 * @param {boolean} bCanCommit (optional)
 */
export interface StartTransactionType {
    transactionName: string;
    bTransactionGroup?: boolean;
    bCanUndo?: boolean;
    bCanCommit?: boolean;
}
